import { DateSearchPipe } from './date-search.pipe';

describe('DateSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
